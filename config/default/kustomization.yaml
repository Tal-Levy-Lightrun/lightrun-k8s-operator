# Adds namespace to all resources.
namespace: lightrun-k8s-operator

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: lightrun-k8s-operator-

# Labels to add to all resources and selectors.
#labels:
#- includeSelectors: true
#  pairs:
#    someName: someValue

resources:
  - ../crd
  - ../rbac
  - ../manager
  - ../webhook
# [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
#- ../prometheus

# No cert-manager: the webhook's TLS serving cert is self-managed at runtime by
# github.com/open-policy-agent/cert-controller (see cmd/main.go), which also keeps this
# MutatingWebhookConfiguration's caBundle patched directly via the API -- no CA-injection
# kustomize patch or cert-manager Certificate/Issuer needed.
patches:
  - path: manager_webhook_patch.yaml

